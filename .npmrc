# @neondatabase/auth-ui pulls @daveyplate/better-auth-ui, whose @better-auth/passkey
# peer range conflicts with the installed version. Accept the resolution so installs
# (local and Vercel CI) don't fail on ERESOLVE.
legacy-peer-deps=true
